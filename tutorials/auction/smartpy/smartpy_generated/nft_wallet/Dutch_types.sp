Storage: sp.TRecord(admin = sp.TAddress, current_id = sp.TNat, tickets = sp.TBigMap(sp.TNat, sp.TTicket(sp.TNat)), token_metadata = sp.TBigMap(sp.TNat, sp.TPair(sp.TNat, sp.TMap(sp.TString, sp.TBytes)))).layout((("admin", "current_id"), ("tickets", "token_metadata")))
Params: sp.TVariant(configNftAuction = sp.TRecord(auction_address = sp.TAddress, opening_price = sp.TNat, reserve_price = sp.TNat, round_time = sp.TInt, start_time = sp.TTimestamp, ticket_id = sp.TNat).layout((("auction_address", ("opening_price", "reserve_price")), ("round_time", ("start_time", "ticket_id")))), createNft = sp.TMap(sp.TString, sp.TBytes), receiveNft = sp.TTicket(sp.TNat), sendNft = sp.TRecord(send_to = sp.TContract(sp.TTicket(sp.TNat)), ticket_id = sp.TNat).layout(("send_to", "ticket_id"))).layout((("configNftAuction", "createNft"), ("receiveNft", "sendNft")))